## What does this change?

<!-- A short description. Link the issue it fixes, e.g. "Fixes #12". -->

## Checklist

- [ ] Tests added or updated (`uv run pytest`)
- [ ] `uv run ruff check .`, `uv run ruff format .` and `uv run mypy` pass
- [ ] README updated if behaviour or options changed
- [ ] Entry added under "Unreleased" in `CHANGELOG.md`
